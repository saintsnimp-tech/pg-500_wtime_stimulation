[project]
name = "pg-500-wtime-stimulation"
version = "0.2.0"
description = "Traceable Australian subclass 500 waiting-time data pipeline"
requires-python = ">=3.10"

[project.scripts]
visa-wait = "visa_wait.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]

